/// <reference types="react" />
import { Entry } from "../../types";
import "./radar.css";
interface RadarProps {
    entries: Entry[];
}
export declare function Radar({ entries }: RadarProps): JSX.Element;
export {};
